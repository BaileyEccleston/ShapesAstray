using UnityEngine;

public class Oven : MonoBehaviour
{

    // whether or not the oven has been moved
    // if it has been moved and is moved again it should not leave behind fire
    public bool slotSet = false;
    public bool beenMoved = false;

}

