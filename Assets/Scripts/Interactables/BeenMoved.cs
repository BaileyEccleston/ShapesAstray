using UnityEngine;

public class BeenMoved : MonoBehaviour
{

    // whether or not the object has been moved
    // if it has been moved and is moved again it should not leave behind anything
    public bool slotSet = false;
    public bool beenMoved = false;

}
