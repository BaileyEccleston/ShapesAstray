using UnityEngine;

public class Sink : MonoBehaviour
{

    // whether or not the sink has been moved
    // if it has been moved and is moved again it should not leave behind water
    public bool slotSet = false;
    public bool beenMoved = false;

}
